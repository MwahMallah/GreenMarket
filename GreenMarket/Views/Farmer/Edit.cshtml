@model ProductEntity

<div class="container">
    <div class="row">
        <h2 class="text-primary">Edit Product</h2>
    </div>
    <form method="post">
        <div class="row gy-2">
            <div class="form-floating">
                <input asp-for="Name" type="text" id="name" 
                       class="form-control" placeholder="Name">
                <label for="name" class="form-label">Name</label>
                <span asp-validation-for="Name" class="text-danger field-validation-error"></span>
            </div>
            <div class="form-floating">
                <input asp-for="ImgUrl" type="text" id="img" 
                       class="form-control" placeholder="Image">
                <label for="img" class="form-label">Image</label>
                <span asp-validation-for="ImgUrl" class="text-danger field-validation-error"></span>
            </div>
            <div class="form-floating">
                <input asp-for="Stock" type="text" id="stock" 
                       class="form-control" placeholder="Stock">
                <label for="stock" class="form-label">Stock</label>
                <span asp-validation-for="Stock" class="text-danger field-validation-error"></span>
            </div>
            <div class="form-group">
                <textarea asp-for="Description" 
                    class="form-control" rows="5">Description</textarea>
                <span asp-validation-for="Description" class="text-danger field-validation-error"></span>
            </div>
            <div class="attributes">
                @foreach (var (attr, index) in Model.Attributes.Select((value, i) => (value, i)))
                {
                    <div class="col-md-6 mb-3">
                        <strong>@attr.Attribute.Name</strong>
                        <input name="Attributes[@index].Value" value="@attr.Value" class="form-control" />
                        <input type="hidden" name="Attributes[@index].Id" value="@attr.Id" class="form-control" />
                        <input type="hidden" name="Attributes[@index].ProductId" value="@attr.ProductId" class="form-control" />
                        <input type="hidden" name="Attributes[@index].AttributeId" value="@attr.AttributeId" class="form-control" />
                    </div>
                }
            </div>
            
            <input asp-for="CategoryId" type="hidden" />
            <input asp-for="CreatorId" type="hidden" />
        </div>
        <div class="row mt-2">
            <div>
                <button type="submit" class="btn btn-primary">Edit</button>
                <a asp-action="Index" class="btn btn-dark">Back to List</a>
            </div>
        </div>
    </form>
</div>